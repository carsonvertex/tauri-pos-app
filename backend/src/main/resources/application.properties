# Server Configuration
server.port=8080

# Multi-Database Configuration

# SQLite Database Configuration (Primary - for local storage)
spring.datasource.sqlite.jdbc-url=jdbc:sqlite:../data/pos_local.db
spring.datasource.sqlite.driver-class-name=org.sqlite.JDBC
spring.datasource.sqlite.username=
spring.datasource.sqlite.password=

# MySQL Database Configuration (Secondary - for adjustment_detail)
# Using remote MySQL database from ERP project
spring.datasource.mysql.jdbc-url=jdbc:mysql://mysql8.dev.rcmart.aws:3306/erp?useSSL=false&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
spring.datasource.mysql.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.mysql.username=admin
spring.datasource.mysql.password=R6OwuPXOPnNDkCyEPAeT

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console disabled (using SQLite)

# Sync Configuration
sync.remote.url=http://localhost:8080
sync.interval=300000
sync.batch-size=100
sync.retry-attempts=3

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# JWT Configuration
jwt.secret=tauri-pos-secret-key-2024
jwt.expiration=86400000

# Logging
logging.level.com.tauri.pos=DEBUG
logging.level.org.springframework.security=DEBUG