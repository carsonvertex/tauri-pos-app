# Server Configuration
server.port=8080

# SQLite Database Configuration
spring.datasource.sqlite.jdbc-url=jdbc:sqlite:../data/pos_local.db
spring.datasource.sqlite.driver-class-name=org.sqlite.JDBC
spring.datasource.sqlite.username=
spring.datasource.sqlite.password=
spring.datasource.sqlite.hikari.maximum-pool-size=5
spring.datasource.sqlite.hikari.minimum-idle=2
spring.datasource.sqlite.hikari.connection-timeout=30000

# MySQL Database Configuration
spring.datasource.mysql.jdbc-url=jdbc:mysql://mysql8.dev.rcmart.aws:3306/erp?useSSL=false&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
spring.datasource.mysql.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.mysql.username=admin
spring.datasource.mysql.password=R6OwuPXOPnNDkCyEPAeT
spring.datasource.mysql.hikari.maximum-pool-size=10
spring.datasource.mysql.hikari.minimum-idle=5
spring.datasource.mysql.hikari.connection-timeout=30000

# JPA Configuration for SQLite (Primary)
spring.datasource.sqlite.jpa.hibernate.ddl-auto=update
spring.datasource.sqlite.jpa.show-sql=true
spring.datasource.sqlite.jpa.properties.hibernate.format_sql=true

# JPA Configuration for MySQL (Secondary)
spring.datasource.mysql.jpa.hibernate.ddl-auto=update
spring.datasource.mysql.jpa.show-sql=true
spring.datasource.mysql.jpa.properties.hibernate.format_sql=true

# H2 Console disabled (using SQLite)

# Sync Configuration
sync.remote.url=http://localhost:8080
sync.interval=300000
sync.batch-size=100
sync.retry-attempts=3

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# JWT Configuration
jwt.secret=tauri-pos-secret-key-2024
jwt.expiration=86400000

# Jackson Configuration - Include null fields in JSON response
spring.jackson.default-property-inclusion=ALWAYS
spring.jackson.serialization.fail-on-empty-beans=false

# Logging
logging.level.com.tauri.pos=DEBUG
logging.level.org.springframework.security=DEBUG